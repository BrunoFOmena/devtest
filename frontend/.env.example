# Exemplo de variaveis de ambiente do frontend (vai pro Git).
# Copie para .env: cp .env.example .env

# URL base da API Rails (Vite expoe so variaveis que comecam com VITE_)
VITE_API_URL=http://localhost:3000
